# System Packages (install via apt):
# sudo apt install -y python3-picamera2  # Required for Raspberry Pi camera support

# Python Packages (install via pip):
Flask==3.0.0
RPi.GPIO==0.7.1  # For GPIO control
psutil==5.9.5
Werkzeug==3.0.1
opencv-python-headless==4.8.1.78  # Lightweight version without GUI dependencies
numpy==1.24.3
gunicorn==21.2.0
colorlog==6.8.0  # For better logging output
Flask-Cors==4.0.0  # For cross-origin requests if needed
